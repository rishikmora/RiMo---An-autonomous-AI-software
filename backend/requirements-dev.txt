# RiMo backend — development, test, and lint tooling.
# Install both for local development:  pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.4
